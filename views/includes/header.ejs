<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3 border-b-[2px] border-zinc-400">

        <% var loggedin = typeof loggedin !== "undefined" ? loggedin : true %>

        <% if(loggedin){ %>
            <a class="text-2xl font-bold" href="/shop">Scatch</a>

            <div class="flex gap-5">
                <a class="text-blue-800" href="/shop">Shop</a>
                <a href="/cart">Cart</a>
                <a href="/myaccount">My account</a>
                <a class="text-red-600" href="/users/logout">Logout</a>
            </div>
        <% } else{ %>
            <div class="flex gap-4 items-center">
                <a class="text-4xl" href="/">Scatch.</a>
                <a class="text-blue-800 mt-1 text-md" href="/owners/admin/login">Are you a Admin ?</a>
            </div>
            
        <% } %>
    </nav>
</body>
</html>